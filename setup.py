"""Setup script for FITS Viewer.

This file exists for backward compatibility with older build tools.
The actual configuration is in pyproject.toml.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
